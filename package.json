{
  "name": "job-khoj",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist server.js",
    "lint": "tsc -p tsconfig.app.json --noEmit",
    "typecheck:node": "tsc -p tsconfig.node.json --noEmit",
    "verify": "npm run lint && npm run typecheck:node && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.115.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "autoprefixer": "^10.4.21",
    "esbuild": "^0.25.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.3"
  }
}
